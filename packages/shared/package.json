{
  "name": "@platform/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "comment:exports": "The `source` condition points tsx (api/worker runtime, run with --conditions=source) straight at the TypeScript source so the running app never loads compiled dist — removes the stale-dist footgun. Default conditions still resolve to dist for tsc + dist consumers. `source` MUST be first.",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./ssrf": {
      "source": "./src/util-ssrf.ts",
      "types": "./dist/util-ssrf.d.ts",
      "import": "./dist/util-ssrf.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist .turbo",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@platform/config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
